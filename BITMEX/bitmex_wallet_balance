#pip install bitmex
import bitmex
client = bitmex.bitmex()
#pip install bravado
import personal

client = bitmex.bitmex(test=False, api_key=, api_secret=)

#client.User.User_getWalletSummary

client.User.User_getMargin().result()

